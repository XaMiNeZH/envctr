# envctr default configuration
# This file is sourced by the main envctr script at startup.

LOG_DIR="${LOG_DIR:-/var/log/envctr}"
DEFAULT_BACKEND="${DEFAULT_BACKEND:-docker}"
MISTRAL_API_KEY="${MISTRAL_API_KEY:-}"
MISTRAL_MODEL="${MISTRAL_MODEL:-mistral-small-latest}"
MISTRAL_API_URL="${MISTRAL_API_URL:-https://api.mistral.ai/v1/chat/completions}"
